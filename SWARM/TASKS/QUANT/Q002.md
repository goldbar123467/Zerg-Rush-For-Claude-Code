# Task: Q002

## Metadata
| Field | Value |
|-------|-------|
| Lane | QUANT |
| Type | ADD_PURE_FN |
| Wave | - |
| Status | PENDING |

## Objective
Create metrics functions: Sharpe, drawdown, turnover.

## Context Pack

### Files
- `src/quant/metrics.py` (create)

### Target Signature
```python
def sharpe_ratio(returns: np.ndarray, risk_free: float = 0.0) -> float:
    """Annualized Sharpe ratio."""

def max_drawdown(equity_curve: np.ndarray) -> float:
    """Maximum drawdown as positive fraction."""

def turnover(positions: np.ndarray) -> float:
    """Average daily turnover."""
```

### Expected Behavior
- Sharpe: annualized (assume daily returns, 252 days)
- Drawdown: peak-to-trough as fraction
- Turnover: mean absolute position change

### Check Command
```bash
python -c "from src.quant.metrics import sharpe_ratio; import numpy as np; print(sharpe_ratio(np.random.randn(100)*0.01))"
```

## Acceptance Criteria (QUANT Gate)
- [ ] Deterministic for fixed seed
- [ ] No NaNs for valid input
