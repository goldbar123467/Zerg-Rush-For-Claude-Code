# Task: Q001

## Metadata
| Field | Value |
|-------|-------|
| Lane | QUANT |
| Type | ADD_STUB |
| Wave | - |
| Status | PENDING |

## Objective
Create small backtest skeleton (single asset, small timeframe).

## Context Pack

### Files
- `src/quant/backtest.py` (create)

### Target Signature
```python
@dataclass
class BacktestResult:
    pnl: np.ndarray
    positions: np.ndarray
    timestamps: np.ndarray

def run_backtest(prices: np.ndarray, signals: np.ndarray) -> BacktestResult:
    """Run simple backtest. Single asset, long-only for now."""
```

### Expected Behavior
- Take price series and signal series
- Signal > 0 = long, signal <= 0 = flat
- Compute PnL from position changes
- Return BacktestResult dataclass

### Check Command
```bash
python -c "from src.quant.backtest import run_backtest; import numpy as np; run_backtest(np.ones(10), np.ones(10))"
```

## Acceptance Criteria (QUANT Gate)
- [ ] Deterministic output for fixed input
- [ ] No NaNs in output
