# Task: M001

## Metadata
| Field | Value |
|-------|-------|
| Lane | ML |
| Type | ADD_PURE_FN |
| Wave | 1 |
| Status | PENDING |
| Created | 2026-01-15 |
| Assigned | - |

## Objective
Create config loader with args schema for ML training.

## Context Pack

### Files
- `src/ml/config.py` (create)

### Target Signature
```python
@dataclass
class TrainConfig:
    batch_size: int = 32
    learning_rate: float = 1e-4
    max_epochs: int = 100
    device: str = "cuda"

def load_config(path: str = None) -> TrainConfig:
    """Load config from YAML or use defaults."""
```

### Expected Behavior
- Dataclass with typed fields
- Load from YAML if path provided
- Fall back to defaults
- Validate types

### Check Command
```bash
python -c "from src.ml.config import load_config; print(load_config())"
```

## Deliverables
- [ ] `src/ml/config.py` created with TrainConfig dataclass
- [ ] `load_config()` function implemented
- [ ] Default values set correctly
- [ ] Type validation working

## Constraints
- Maximum file size: 100 lines
- Maximum files to create: 2 files

## Acceptance Criteria (ML Gate)
- [ ] No import breaks
- [ ] Config loads with defaults
