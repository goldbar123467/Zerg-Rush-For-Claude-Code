# Task: M003

## Metadata
| Field | Value |
|-------|-------|
| Lane | ML |
| Type | ADD_PURE_FN |
| Wave | 1 |
| Status | PENDING |
| Created | 2026-01-15 |
| Assigned | - |

## Objective
Create logging + metric collector for training.

## Context Pack

### Files
- `src/ml/metrics.py` (create)

### Target Signature
```python
class MetricCollector:
    def __init__(self):
        self.metrics = defaultdict(list)

    def log(self, name: str, value: float, step: int):
        """Log a metric value."""

    def get_summary(self) -> dict:
        """Return mean of each metric."""
```

### Expected Behavior
- Store metrics by name
- Compute running mean
- Thread-safe not required (single process)
- Print summary on request

### Check Command
```bash
python -c "from src.ml.metrics import MetricCollector; m = MetricCollector(); m.log('loss', 0.5, 1); print(m.get_summary())"
```

## Deliverables
- [ ] `src/ml/metrics.py` created with MetricCollector class
- [ ] `log()` method implemented for storing metrics
- [ ] `get_summary()` method returns mean values
- [ ] Running mean calculation correct

## Constraints
- Maximum file size: 100 lines
- Maximum files to create: 2 files

## Acceptance Criteria (ML Gate)
- [ ] Metrics log correctly
- [ ] Summary returns dict
