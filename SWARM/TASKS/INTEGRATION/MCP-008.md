# Task: MCP-008

## Metadata
| Field | Value |
|-------|-------|
| Lane | INTEGRATION |
| Type | ADD_PURE_FN |
| Wave | - |
| Status | PENDING |
| Created | 2026-01-15 |
| Assigned | - |

## Context Pack

### Files
- /home/ubuntu/projects/zerg-swarm/src/zerg_swarm_mcp/tools/zergling.py (create)

### Target Signature
```python
@mcp.tool(name="zergling_register", description="Register a new zergling")
async def zergling_register(ctx: Context, name: str) -> dict:
    """Register an active zergling with the swarm."""

@mcp.tool(name="zergling_unregister", description="Unregister a zergling")  
async def zergling_unregister(ctx: Context, name: str) -> dict:
    """Remove a zergling from active list."""

@mcp.tool(name="zergling_list", description="List active zerglings")
async def zergling_list(ctx: Context) -> list[dict]:
    """Return all active zerglings with their metadata."""

@mcp.tool(name="zergling_get", description="Get zergling info")
async def zergling_get(ctx: Context, name: str) -> dict:
    """Get details about a specific zergling."""
```

### Expected Behavior
- Zerglings stored in STATE.json active_zerglings array
- Register adds entry with name, timestamp, current wave
- Unregister removes entry by name
- List returns all active zerglings

### Check Command
```bash
python -c "from zerg_swarm_mcp.tools.zergling import zergling_list"
```

## Objective
Create MCP tools for zergling lifecycle management.

## Deliverables
- [ ] Implement zergling_register
- [ ] Implement zergling_unregister
- [ ] Implement zergling_list
- [ ] Implement zergling_get
- [ ] Update STATE.json atomically

## Acceptance Criteria (INTEGRATION Gate)
- [ ] Registration persists to STATE.json
- [ ] Unregister removes correct entry
- [ ] List returns accurate data

## Constraints
- Max 80 lines
- 1 file
