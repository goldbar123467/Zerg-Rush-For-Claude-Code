# Task: MCP-002

## Metadata
| Field | Value |
|-------|-------|
| Lane | INTEGRATION |
| Type | ADD_STUB |
| Wave | - |
| Status | PENDING |
| Created | 2026-01-15 |
| Assigned | - |

## Context Pack

### Files
- /home/ubuntu/projects/zerg-swarm/src/zerg_swarm_mcp/__init__.py (create)
- /home/ubuntu/projects/zerg-swarm/src/zerg_swarm_mcp/config.py (create)

### Target Signature
```python
# config.py
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    swarm_root: str = "/home/ubuntu/projects/zerg-swarm/SWARM"
    host: str = "127.0.0.1"
    port: int = 8766
    
settings = Settings()
```

### Expected Behavior
- __init__.py exports version and Settings
- config.py has Settings class with swarm paths
- Uses pydantic-settings for env var support

### Check Command
```bash
python -c "from zerg_swarm_mcp.config import settings; print(settings.swarm_root)"
```

## Objective
Create package init and settings configuration for the MCP server.

## Deliverables
- [ ] Create src/zerg_swarm_mcp/ directory structure
- [ ] Create __init__.py with version
- [ ] Create config.py with Settings class
- [ ] Support SWARM_ROOT env var override

## Acceptance Criteria (INTEGRATION Gate)
- [ ] Import works without error
- [ ] Settings loads default values

## Constraints
- Max 60 lines total
- 2 files
