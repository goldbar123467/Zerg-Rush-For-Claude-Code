# Task: MCP-009

## Metadata
| Field | Value |
|-------|-------|
| Lane | INTEGRATION |
| Type | ADD_PURE_FN |
| Wave | - |
| Status | PENDING |
| Created | 2026-01-15 |
| Assigned | - |

## Context Pack

### Files
- /home/ubuntu/projects/zerg-swarm/src/zerg_swarm_mcp/tools/wave.py (create)

### Target Signature
```python
@mcp.tool(name="wave_status", description="Get current wave info")
async def wave_status(ctx: Context) -> dict:
    """Return current wave number and statistics."""

@mcp.tool(name="wave_increment", description="Advance to next wave")
async def wave_increment(ctx: Context) -> dict:
    """Increment wave counter and return new wave number."""

@mcp.tool(name="wave_collect", description="Collect results from INBOX")
async def wave_collect(ctx: Context) -> dict:
    """Process results from INBOX and update completed_tasks."""
```

### Expected Behavior
- wave_status returns wave number + task counts
- wave_increment atomically increments wave in STATE.json
- wave_collect moves results from INBOX to completed_tasks

### Check Command
```bash
python -c "from zerg_swarm_mcp.tools.wave import wave_status"
```

## Objective
Create MCP tools for wave lifecycle management.

## Deliverables
- [ ] Implement wave_status with statistics
- [ ] Implement wave_increment with atomic update
- [ ] Implement wave_collect to process INBOX

## Acceptance Criteria (INTEGRATION Gate)
- [ ] wave_status returns accurate counts
- [ ] wave_increment persists new wave number

## Constraints
- Max 70 lines
- 1 file
