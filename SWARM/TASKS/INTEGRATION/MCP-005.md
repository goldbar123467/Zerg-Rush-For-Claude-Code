# Task: MCP-005

## Metadata
| Field | Value |
|-------|-------|
| Lane | INTEGRATION |
| Type | ADD_PURE_FN |
| Wave | - |
| Status | PENDING |
| Created | 2026-01-15 |
| Assigned | - |

## Context Pack

### Files
- /home/ubuntu/projects/zerg-swarm/src/zerg_swarm_mcp/tools/__init__.py (create)
- /home/ubuntu/projects/zerg-swarm/src/zerg_swarm_mcp/tools/state.py (create)

### Target Signature
```python
from fastmcp import Context
from ..server import mcp
from ..config import settings
import json
from pathlib import Path

STATE_FILE = Path(settings.swarm_root) / "STATE.json"

@mcp.tool(name="swarm_status", description="Get current swarm state")
async def swarm_status(ctx: Context) -> dict:
    """Return current swarm state including wave, zerglings, and tasks."""
    with open(STATE_FILE) as f:
        return json.load(f)

@mcp.tool(name="swarm_reset", description="Reset swarm to initial state")
async def swarm_reset(ctx: Context) -> dict:
    """Reset all swarm state to initial values."""
    state = {"wave": 0, "active_zerglings": [], "completed_tasks": [], "pending_tasks": [], "last_updated": ""}
    with open(STATE_FILE, 'w') as f:
        json.dump(state, f, indent=2)
    return {"status": "reset", "state": state}
```

### Expected Behavior
- swarm_status returns current STATE.json contents
- swarm_reset clears all state to defaults
- Both tools handle file errors gracefully

### Check Command
```bash
python -c "from zerg_swarm_mcp.tools.state import swarm_status, swarm_reset"
```

## Objective
Create MCP tools for reading and resetting swarm state.

## Deliverables
- [ ] Create tools/__init__.py
- [ ] Implement swarm_status tool
- [ ] Implement swarm_reset tool
- [ ] Add error handling for missing STATE.json

## Acceptance Criteria (INTEGRATION Gate)
- [ ] Tools register with MCP server
- [ ] swarm_status returns valid JSON
- [ ] swarm_reset clears state

## Constraints
- Max 60 lines
- 2 files
