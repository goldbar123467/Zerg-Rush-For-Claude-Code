# Task: DOC-001

## Metadata
| Field | Value |
|-------|-------|
| Lane | INTEGRATION |
| Type | DOC_SNIPPET |
| Wave | - |
| Status | PENDING |
| Created | 2026-01-15 |
| Assigned | - |

## Context Pack

### Files
- `/home/ubuntu/projects/zerg-swarm/README.md` (lines 90-130)
- `/home/ubuntu/projects/zerg-swarm/SWARM/`

### Current README Section (Incorrect)
```markdown
### Project Structure

\`\`\`mermaid
graph TD
    subgraph SWARM["📁 SWARM/"]
        STATE["STATE.json"]
        RULES["SWARM_RULES.md"]
        GATES["GATES.md"]
        PROMPTS["PROMPTS.md"]

        subgraph TASKS["📁 TASKS/"]
            KERNEL["🔥 KERNEL/"]
            ML["🧠 ML/"]
            QUANT["📊 QUANT/"]
            DEX["💱 DEX/"]
            INT["🔗 INTEGRATION/"]
        end

        subgraph IO["📁 I/O"]
            INBOX["📥 INBOX/"]
            OUTBOX["📤 OUTBOX/"]
        end

        TEMPLATES["📁 TEMPLATES/"]
        SCRIPTS["📁 SCRIPTS/"]
        LOCKS["🔒 LOCKS/"]
    end

    STATE --> TASKS
    TASKS --> IO
\`\`\`
```

### Actual Directory Structure
```
SWARM/
├── STATE.json
├── SWARM_RULES.md
├── GATES.md
├── PROMPTS.md
├── RUNBOOK.md
├── OVERLORD.md
├── ZERGLING.md
├── TASKS/
│   ├── KERNEL/          (K001.md, K002.md, K003.md, README.md)
│   ├── ML/              (M001.md, M002.md, M003.md, README.md)
│   ├── QUANT/           (Q001.md, Q002.md, Q003.md, README.md)
│   ├── DEX/             (D001.md, D002.md, D003.md, README.md)
│   ├── INTEGRATION/     (INT-001.md through INT-008.md, README.md)
│   ├── EXAMPLE-001.md
│   ├── T001.md
│   └── TEMPLATE.md
├── INBOX/               (exists as empty dir)
├── OUTBOX/              (exists as empty dir)
├── LOCKS/               (exists as empty dir)
├── RESULTS/             (exists as empty dir - conflicts with INBOX naming)
├── TEMPLATES/
│   ├── TASK_TEMPLATE.md
│   └── RESULT_TEMPLATE.md
└── SCRIPTS/
    └── swarm.py
```

### Issues Identified
1. **README mermaid diagram is incomplete**: Missing RESULTS directory, RUNBOOK.md, OVERLORD.md, ZERGLING.md
2. **Missing documentation of actual directories**: INBOX/, OUTBOX/, LOCKS/ exist but aren't shown
3. **RESULTS vs INBOX naming conflict**: RESULTS/ exists but documentation primarily refers to INBOX/
4. **STATE.json path ambiguity**: Documented at root in some places, actually at SWARM/STATE.json
5. **Missing file listings**: Lane directories should document actual task files

### Expected Behavior
- README documents exactly what exists
- All existing directories are shown
- All documented directories exist
- No references to non-existent paths
- Clear distinction between RESULTS/ and INBOX/

### Check Command
```bash
# Verify documented structure matches reality
cd /home/ubuntu/projects/zerg-swarm
echo "=== Documented in README ===" && grep -A 30 "### Project Structure" README.md | head -35
echo ""
echo "=== Actual directories ===" && find SWARM -maxdepth 2 -type d | sort
echo ""
echo "=== Check for undocumented files ===" && ls -la SWARM/ | grep -E "\.md$" | grep -v "^d"
```

## Objective
Update README.md "Project Structure" section to accurately document the actual SWARM directory layout, removing references to non-existent directories and adding missing components.

## Deliverables
- [ ] Update the mermaid diagram to match actual directory structure
- [ ] Add RESULTS/ and RUNBOOK.md/OVERLORD.md/ZERGLING.md to the diagram
- [ ] Add notation for empty working directories (INBOX, OUTBOX, LOCKS, RESULTS)
- [ ] Add a text-based tree structure as backup to mermaid
- [ ] Remove any references to `tasks/`, `agents/`, `output/` directories
- [ ] Add note clarifying RESULTS/ vs INBOX/ naming
- [ ] Add note clarifying STATE.json is at SWARM/STATE.json
- [ ] Verify no broken links in updated text

## Acceptance Criteria (INTEGRATION Gate)
- [ ] Wire test: README structure section renders without syntax errors
- [ ] Verification: `find SWARM -maxdepth 2 -type d` matches documented structure
- [ ] Validation: No references to non-existent directories remain

## Constraints
- Max 100 lines of new code
- Max 2 files (README.md + optional supporting doc)
- No architectural changes
- No new directories needed

## Notes
This is a CRITICAL flaw per FLAWS.md (Documentation - CRITICAL section). The README actively misleads users about project structure. This task should unify the documented structure with reality. Consider whether RESULTS/ should be renamed to INBOX/ for consistency, or if both names serve different purposes that should be documented.

Key reference files:
- `/home/ubuntu/projects/zerg-swarm/FLAWS.md` (line: "Directory structure mismatch - README.md references `tasks/`, `agents/`, `output/` directories that don't exist")
- CLAUDE.md Agent Mail protocol (mentions INBOX/OUTBOX file pattern)
- OVERLORD.md (references RESULTS/ directory)
