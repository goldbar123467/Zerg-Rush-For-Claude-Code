# Task: MCP-006

## Metadata
| Field | Value |
|-------|-------|
| Lane | INTEGRATION |
| Type | ADD_PURE_FN |
| Wave | - |
| Status | PENDING |
| Created | 2026-01-15 |
| Assigned | - |

## Context Pack

### Files
- /home/ubuntu/projects/zerg-swarm/src/zerg_swarm_mcp/tools/tasks.py (create)

### Target Signature
```python
@mcp.tool(name="task_list", description="List tasks by lane or status")
async def task_list(ctx: Context, lane: str = None, status: str = None) -> list[dict]:
    """List task cards, optionally filtered by lane or status."""

@mcp.tool(name="task_get", description="Get a specific task card")
async def task_get(ctx: Context, task_id: str, lane: str) -> dict:
    """Read and return a task card's contents."""

@mcp.tool(name="task_create", description="Create a new task card")
async def task_create(ctx: Context, task_id: str, lane: str, type: str, objective: str) -> dict:
    """Create a new task card in the specified lane."""
```

### Expected Behavior
- task_list scans TASKS/{lane}/ directories for .md files
- task_get reads specific task card and parses metadata
- task_create writes new task card using template format
- All return structured JSON responses

### Check Command
```bash
python -c "from zerg_swarm_mcp.tools.tasks import task_list"
```

## Objective
Create MCP tools for listing, reading, and creating task cards.

## Deliverables
- [ ] Implement task_list with lane/status filtering
- [ ] Implement task_get to read task cards
- [ ] Implement task_create with template format
- [ ] Parse task card metadata into structured dict

## Acceptance Criteria (INTEGRATION Gate)
- [ ] task_list returns list of tasks
- [ ] task_get returns task dict
- [ ] task_create writes valid task card

## Constraints
- Max 100 lines
- 1 file
