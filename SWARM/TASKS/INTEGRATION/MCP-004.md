# Task: MCP-004

## Metadata
| Field | Value |
|-------|-------|
| Lane | INTEGRATION |
| Type | ADD_STUB |
| Wave | - |
| Status | PENDING |
| Created | 2026-01-15 |
| Assigned | - |

## Context Pack

### Files
- /home/ubuntu/projects/zerg-swarm/src/zerg_swarm_mcp/server.py (create)

### Target Signature
```python
from fastmcp import FastMCP
from .config import settings

mcp = FastMCP(
    name="zerg-swarm",
    instructions="Zerg Swarm coordination server for multi-agent task management."
)

# Import tools to register them
from .tools import state, tasks, locks, zergling, wave, results

def main():
    import uvicorn
    uvicorn.run(mcp.http_app(), host=settings.host, port=settings.port)

if __name__ == "__main__":
    main()
```

### Expected Behavior
- FastMCP server initialized with name and instructions
- Tools imported and registered automatically
- HTTP server runs on configured host:port
- Entry point callable from CLI

### Check Command
```bash
python -c "from zerg_swarm_mcp.server import mcp; print(mcp.name)"
```

## Objective
Create FastMCP server setup with HTTP transport and tool registration.

## Deliverables
- [ ] Initialize FastMCP with name "zerg-swarm"
- [ ] Add server instructions
- [ ] Import tool modules to register tools
- [ ] Create main() entry point with uvicorn

## Acceptance Criteria (INTEGRATION Gate)
- [ ] Server imports without error
- [ ] mcp.name returns "zerg-swarm"

## Constraints
- Max 50 lines
- 1 file
