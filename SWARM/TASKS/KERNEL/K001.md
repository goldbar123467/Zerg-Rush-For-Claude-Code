# Task: K001

## Metadata
| Field | Value |
|-------|-------|
| Lane | KERNEL |
| Type | ADD_PURE_FN |
| Wave | - |
| Status | PENDING |

## Objective
Create CPU reference implementation for matrix multiply (small shapes only).

## Context Pack

### Files
- `src/kernel/reference.py` (create)

### Target Signature
```python
def matmul_cpu_ref(a: np.ndarray, b: np.ndarray) -> np.ndarray:
    """CPU reference for matmul. Small shapes only (≤512x512)."""
```

### Expected Behavior
- Pure NumPy implementation
- Support 2D float32 arrays
- Assert shape compatibility
- Return float32 result

### Check Command
```bash
python -c "from src.kernel.reference import matmul_cpu_ref; import numpy as np; print(matmul_cpu_ref(np.eye(4), np.eye(4)))"
```

## Acceptance Criteria (KERNEL Gate)
- [ ] Correctness: matches np.matmul output
- [ ] Runs without error for 64x64, 128x128, 256x256
