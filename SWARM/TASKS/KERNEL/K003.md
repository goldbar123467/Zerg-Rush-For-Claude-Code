# Task: K003

## Metadata
| Field | Value |
|-------|-------|
| Lane | KERNEL |
| Type | ADD_BENCH |
| Wave | - |
| Status | PENDING |

## Objective
Create minimal benchmark harness for matmul (1-2 shapes).

## Context Pack

### Files
- `benchmarks/kernel/bench_matmul.py` (create)

### Target Signature
```python
def bench_matmul(shape: tuple, warmup: int = 3, runs: int = 10) -> dict:
    """Benchmark matmul for given shape. Returns timing stats."""
```

### Expected Behavior
- Warmup runs (discard)
- Timed runs with time.perf_counter
- Return dict: {mean_ms, std_ms, shape}
- Print results

### Check Command
```bash
python benchmarks/kernel/bench_matmul.py
```

## Acceptance Criteria (KERNEL Gate)
- [ ] Runs for shape (256, 256)
- [ ] Prints timing in milliseconds
